<Window x:Class="ConfigManager.NewJob"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConfigManager"
        mc:Ignorable="d"
        Title="Job anlegen" 
        Height="500" 
        Width="600" 
        ResizeMode="CanMinimize">
    <Grid>
        <TabControl>
            <TabItem Header="Name &amp; Agents">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                        <Label FontWeight="Bold">Name:</Label>
                        <TextBox Width="522" Margin="5,0,0,0"></TextBox>
                    </StackPanel>

                    <GroupBox Grid.Row="1" Header="Agent" Padding="5" Margin="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Label Grid.Column="0" FontWeight="Bold">Name/IP:</Label>
                            <TextBox Grid.Column="1" Margin="5,0,0,0"></TextBox>
                            <Button Grid.Column="2" Margin="5,0" Width="50">Add</Button>
                        </Grid>
                    </GroupBox>
                    
                    <ScrollViewer Grid.Row="2" Name="AddedAgents" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto">
                        <ListView Name="AgentAddView">
                            <local:AgentProcessOverview></local:AgentProcessOverview>
                        </ListView>
                    </ScrollViewer>

                    <Button Grid.Row="3" Content="Weiter" HorizontalAlignment="Right" Width="100" Margin="5"></Button>
                </Grid>
            </TabItem>
            
            <TabItem Header="Content" IsEnabled="False">
                <Label Content="Content goes here..." />
            </TabItem>
            
            <TabItem Header="Action" IsEnabled="False">
                <Label Content="Content goes here..." />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
