<UserControl x:Class="ConfigManager.TimeLineControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConfigManager"
             mc:Ignorable="d">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Name="LyoutWindowNumber" FontWeight="Bold" FontSize="24" VerticalAlignment="Center"></Label>
        <CheckBox Grid.Column="1" Name="Loop" Margin="5,0" VerticalAlignment="Center">Schleife</CheckBox>
        <ListBox Grid.Column="2" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Name="TimeLineItems" Height="100">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            
            
        </ListBox>
    </Grid>
</UserControl>
