<Window x:Class="ConfigManager.UrlDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConfigManager"
        mc:Ignorable="d"
        Title="Url"
        Width="400"
        Height="135"
        Icon="/ConfigManager;component/Resources/Icon.ico" 
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Label FontWeight="Bold" Grid.Column="0" Grid.Row="0">URL</Label>
        <TextBox Name="UrlInput" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" Width="380" Margin="5">http://</TextBox>
        
        <Button Name="Ok" Grid.Column="1" Grid.Row="2" Margin="5" Width="50" HorizontalAlignment="Right" Click="Ok_Click">Ok</Button>
        <Button Name="Cancle"  Grid.Column="2" Grid.Row="2" Margin="5" Width="90" HorizontalAlignment="Right" Click="Cancle_Click">Abbrechen</Button>
    </Grid>
</Window>
