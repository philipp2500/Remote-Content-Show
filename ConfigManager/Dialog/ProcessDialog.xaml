<Window x:Class="ConfigManager.ProcessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConfigManager"
        mc:Ignorable="d"
        Title="Process" 
        Width="610"
        Height="190"
        Icon="/ConfigManager;component/Resources/Icon.ico" 
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>        

        <ComboBox Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Margin="5,5,5,0" Height="110" Width="580" Name="ProcessList" SelectionChanged="ProcessList_SelectionChanged">
        </ComboBox>
        
        <Button Name="Ok" Grid.Column="0" Grid.Row="1" Margin="5" Width="50" HorizontalAlignment="Right" Click="Ok_Click">Ok</Button>
        <Button Name="Cancle"  Grid.Column="1" Grid.Row="1" Margin="5" Width="90" HorizontalAlignment="Right" Click="Cancle_Click">Abbrechen</Button>        
    </Grid>
</Window>
